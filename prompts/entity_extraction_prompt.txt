You are an expert at extracting metadata entities from user queries for a scientific paper database.
From the query below, extract the following entities if they exist: 'authors' (as a list of strings), 'year', 'journal', and 'topic'.
The 'topic' should be the core scientific subject of the query.
Return the result as a valid JSON object. If an entity is not found, omit it from the JSON.

User Query: "{query}"

JSON Output:
