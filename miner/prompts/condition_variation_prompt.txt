You are an expert chemical data extractor.
You are given a list of STANDARD reaction conditions in JSON, and a VARIATION text that describes how the conditions for this specific entry deviate from the standard.

Your task is to return the UPDATED list of conditions as a single JSON array, modifying, adding, or deleting the standard conditions according to the variation.

STANDARD CONDITIONS:
{base_conditions}

VARIATION TEXT:
{variation_text}

Rules:
1. **Substitution ("Y instead of X" / "Y replaced X"):** Remove X and add Y with the same role as X (or infer the role from Y's chemical identity).
2. **Subtraction ("Without X" / "no X" / "omitting X" / "absence of X"):** Remove every condition that matches X from the list. If multiple components are omitted (e.g. "no photocatalyst, no nickel, no light"), remove all of them.
3. **Addition ("with X added" / "+ X"):** Append a new condition entry for X with an appropriate role.
4. **Update ("20 °C" / "2 h"):** Update the matching temperature or time condition.
5. **None / Standard ("none" / "—" / "standard"):** Return the standard conditions unchanged.
6. **Control entries ("no catalyst, no light, dark conditions"):** These are blank/negative controls — remove ALL the omitted components from the list. Be thorough: if the variation says three things are absent, remove all three.

Important:
- ONLY return a syntactically valid JSON array of objects.
- Each object MUST retain "role" and "text" keys.
- Retain any existing "quantity", "unit", or "smiles" keys when they remain valid after the variation.
- Do NOT include markdown code blocks, comments, or explanations — raw JSON only.
